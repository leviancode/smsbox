<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="group"
            type="com.leviancode.android.gsmbox.data.model.TemplateGroupObservable" />

        <variable
            name="viewModel"
            type="com.leviancode.android.gsmbox.ui.templates.viewmodel.TemplateGroupListViewModel" />
    </data>


        <com.google.android.material.card.MaterialCardView
            android:id="@+id/card_template_group"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:layout_marginTop="4dp"
            android:layout_marginBottom="4dp"
            android:onClick="@{() -> viewModel.onItemClick(group.model)}"
            app:cardCornerRadius="6dp"
            android:contentDescription="@string/open_group"
            android:clickable="true"
            android:focusable="true">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <androidx.appcompat.widget.AppCompatImageView
                    android:id="@+id/template_group_image"
                    android:layout_width="42dp"
                    android:layout_height="42dp"
                    android:padding="4dp"
                    android:layout_marginStart="8dp"
                    android:scaleType="fitCenter"
                    android:layout_gravity="center_vertical"
                    android:tint="@{group.iconColor}"
                    app:loadImage="@{group.imageUri}"
                    app:srcCompat="@drawable/ic_baseline_source_24" />

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_weight="1"
                    android:layout_height="wrap_content"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/tv_template_group_name"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="12dp"
                        android:layout_marginTop="16dp"
                        android:text="@{group.name}"
                        android:textColor="@color/text_title"
                        android:textSize="18sp"
                        android:textStyle="bold"
                        tools:text="Title" />

                    <TextView
                        android:id="@+id/tv_template_group_description"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="12dp"
                        android:layout_marginTop="8dp"
                        android:layout_marginBottom="16dp"
                        android:text="@{group.description}"
                        android:textColor="@color/text_description"
                        tools:text="Description" />

                </LinearLayout>

                <TextView
                    android:id="@+id/tv_templates_count"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center_vertical"
                    android:layout_marginEnd="8dp"
                    android:text="@{group.size}"
                    android:textSize="14sp"
                    tools:text="8" />

                <androidx.appcompat.widget.AppCompatImageButton
                    android:id="@+id/ib_popup_menu"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center_vertical"
                    android:layout_marginEnd="8dp"
                    android:contentDescription="@string/open_option_menu"
                    android:onClick="@{(view) -> viewModel.onPopupMenuClick(view, group.model)}"
                    android:padding="4dp"
                    android:background="?android:selectableItemBackgroundBorderless"
                    android:src="@drawable/ic_baseline_more_vert_24" />

            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

</layout>
