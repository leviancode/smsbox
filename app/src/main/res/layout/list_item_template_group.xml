<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="model"
            type="com.leviancode.android.gsmbox.ui.entities.templates.TemplateGroupUI" />

        <variable
            name="viewModel"
            type="com.leviancode.android.gsmbox.ui.screens.templates.groups.list.TemplateGroupListViewModel" />
    </data>


        <com.google.android.material.card.MaterialCardView
            android:id="@+id/card_template_group"
            android:layout_width="match_parent"
            android:layout_height="@dimen/template_group_card_height"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="4dp"
            android:onClick="@{() -> viewModel.onItemClick(model)}"
            app:cardCornerRadius="6dp"
            android:contentDescription="@string/open_group"
            android:clickable="true"
            android:focusable="true">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="horizontal">

                <androidx.appcompat.widget.AppCompatImageView
                    android:id="@+id/template_group_image"
                    android:layout_width="42dp"
                    android:layout_height="42dp"
                    android:padding="4dp"
                    android:layout_marginStart="8dp"
                    android:scaleType="fitCenter"
                    android:layout_gravity="center_vertical"
                    app:srcCompat="@drawable/ic_baseline_source_24"
                    app:setImageTintGroup="@{model.iconColor}"
                    tools:tint="@color/default_group_icon_color" />

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_weight="1"
                    android:gravity="center_vertical"
                    android:layout_height="match_parent"
                    android:orientation="vertical">

                    <com.google.android.material.textview.MaterialTextView
                        android:id="@+id/tv_template_group_name"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="12dp"
                        android:text="@{model.name}"
                        android:textColor="@color/text_title"
                        android:textSize="18sp"
                        android:textStyle="bold"
                        tools:text="Title" />

                    <com.google.android.material.textview.MaterialTextView
                        android:id="@+id/tv_template_group_description"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="12dp"
                        android:layout_marginTop="6dp"
                        android:visibility="visible"
                        app:visibilityDependingOnText="@{model.description}"
                        android:text="@{model.description}"
                        android:textColor="@color/text_description"
                        tools:text="Description" />

                </LinearLayout>

                <com.google.android.material.textview.MaterialTextView
                    android:id="@+id/tv_templates_count"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center_vertical"
                    android:layout_marginEnd="8dp"
                    android:text="@{model.size}"
                    android:textSize="14sp"
                    tools:text="8" />

                <androidx.appcompat.widget.AppCompatImageButton
                    android:id="@+id/ib_popup_menu"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center_vertical"
                    android:layout_marginEnd="@dimen/popup_btn_margin_end"
                    android:contentDescription="@string/open_option_menu"
                    android:onClick="@{(view) -> viewModel.onPopupMenuClick(view, model)}"
                    android:padding="4dp"
                    android:tint="@color/popup_menu"
                    android:background="?android:selectableItemBackgroundBorderless"
                    android:src="@drawable/ic_baseline_more_vert_24" />

            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

</layout>
